# style name or JSON path (default "auto")
style: "/home/jalhamer/.config/glow/catppuccin-macchiato.json"
# mouse support (TUI-mode only)
mouse: true
# use pager to display markdown
pager: true
# word-wrap at width
width: 88
